// Function to multiply numbers
function times(num1, num2) {
    return num1 * num2;
}

// Use this way
const number1 = 8;
const number2 = 6;

// Call the function and store the result in a variable
const result = times(number1, number2);

// Output the result to the console
console.log(result);
